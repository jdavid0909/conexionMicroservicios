server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: circuit-breaker
          uri: https://github.com/jdavid0909/conexionMicroservicios.git
          search-paths: config-data
  application:
    name: config-service
